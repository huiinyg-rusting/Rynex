[package]
name = "niobix-kernel"
version = "0.1.0"
edition = "2021"

[dependencies]
x86_64 = { version = "0.15", features = ["nightly"] }

[profile.release]
panic = "abort"
opt-level = "z"
lto = true
codegen-units = 1